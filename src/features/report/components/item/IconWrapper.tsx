import {ReportIconItem} from "@/features/report/reportTypes.ts";

type Props = {
  item: ReportIconItem,
}

export function IconWrapper({item}: Props) {

  function getIcon() {
    switch (item.itemType) {

    }
  }

  return (
    <></>
  );
}